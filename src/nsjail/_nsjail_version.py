"""nsjail binary version (upstream).

This is separate from the Python package version (managed by setuptools_scm from git tags).
Update this when bundling a new nsjail binary.
"""
__nsjail_version__: str = "3.6"
